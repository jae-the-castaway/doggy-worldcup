import { Lilita_One } from "next/font/google";
import { Noto_Sans } from "next/font/google";

export const lilitaOne = Lilita_One({ weight: "400", subsets: ["latin"] });
export const notoSans = Noto_Sans({ weight: ["400", "700"], subsets: ["latin"] });
